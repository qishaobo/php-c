// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("shop", "for shop support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("shop", "enable shop support", "no");

if (PHP_SHOP != "no") {
	EXTENSION("shop", "shop.c");
}

